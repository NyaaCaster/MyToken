# MyToken V1 阶段交接 001

## 交接目的

我的项目，由 Agent 团队按 SSOT 计划分 P 完成。本文汇总当前进度与续接要点，供下一对话/部署阶段直接续接。

必读文档（依序）：
1. `H:\GitHub\MyToken\.docs\开发计划-SSOT.md`（蓝图）
2. `H:\GitHub\MyToken\.docs\设计审计.md`（架构与决策）
3. `H:\GitHub\MyToken\.docs\代码审核报告.md`（质检清单）
4. 本文件（当前进度）

## 当前进度（V1 开发已完成）

- ✅ P1 工程化基础：meta.json / package.json / tsconfig / vite / index.html(data-blessing) / src/main/version/css / .gitignore / .gitattributes / LICENSE(AGPL-3.0) / README / .env.example
- ✅ P2 后端代理层：`server/src/{index,providers}.ts`（Express4，`/api/providers/:id/query`，域名白名单、错误归一、密钥不落日志、HOST/PORT 可配、CORS 白名单收敛）
- ✅ P3 前端骨架与主题：Header(Logo+名跳仓库) / ThemeToggle(浅/暗/跟随系统) / useTheme / 毛玻璃+蓝紫渐变+NyaaChat 美术基调 / 蓝色滚动条
- ✅ P4 适配器层 + 四家核心：`src/types/provider.ts` / `providers/registry.ts` / `providers/query.ts` / `hooks/{useProvidersConfig,useBalances}` / `components/ProviderModule`；QinyAPI/DeepSeek/OpenCode Go/硅基流动
- ✅ P5 Coding Plan：后端 5 家端点（anthropic/zai/minimax/kimi/openrouter）+ 前端注册 + query 适配器
- ✅ P6 交互：SecretInput(密钥隐藏) / BaseModal / ProviderDocModal(md 浮窗) / 开关鉴权联动 / 模块收起(motion) / 顶栏留位；4 家 md（qinyapi/deepseek/opencode-go/siliconflow）+ 5 家 coding-plan md
- ✅ P7 产物：Dockerfile / nginx.conf / docker-compose*.yml / rebuild.py / restart.py / README.dev.md / .dockerignore
- ✅ 代码审核：reviewer 通过，问题 #1–#4 已修复（Coding Plan md 补齐、CORS 收敛+HOST 可配、浮窗可重试、README 重写）

## 尚未进行 / 已知待办（续接后第一优先）

1. **联网 `npm install` 并构建冒烟**：本机会话全程离线，未装依赖、未跑 `tsc`/`vite build`。有网后先 `npm i`，再 `npm run typecheck:server` 与 `npm run build` 复核类型/编译（审核标注此为复检项）。
2. **真实 Key 对拍**：四家核心 + 5 家 Coding Plan 用真实密钥各发一次验证字段名与归一（`.ref/` 调研已指出个别字段以实测为准）。
3. **git 分阶段提交**：本地 git 已完成 init；按 P 分批 commit（本交接交付前应已提交，若未完成在此补）。推送到 `https://github.com/NyaaCaster/MyToken.git` 需 `$GITHUB_PAT` 临时鉴权（本机无外网可能受阻，需告知用户）。
4. **部署**：`rebuild.py` 构建→推送私有仓库（需 `PRIVATE_DOCKER_REGISTRY_HOST` 的 `.env`）→ macmini `restart.py` 上线。本机无外网/未配 `.env`，需用户在有网环境执行或授权。

## 本轮关键决策回顾

- 架构：前端 SPA + 轻量 Express 代理层（规避 CORS/UA/Key 保护）。
- DeepSeek 仅余额、硅基流动仅余额（官方无统计 API）。
- Codin 命名保持 `MyToken`（Nyaa 前缀例外）；V1 含四家核心 + 5 家 Coding Plan（不含 SCNet）。
- 密钥前端 localStorage 隐藏显示；代理白名单不落日志。

## 密钥/承诺

- 密钥不提交、不上传；`.ref/` 不入 git；`.docs/` 入 git；`.env.example` 仅占位。

---

## 续接提示词

继续开发 MyToken，请以 plan 模式推进，依序读取：
`H:\GitHub\MyToken\CLAUDE.md`（若存在，否则读工作区 `H:\GitHub\CLAUDE.md`）→
`H:\GitHub\MyToken\.docs\开发计划-SSOT.md` →
`H:\GitHub\MyToken\.docs\阶段交接-001.md`。

当前进度：V1 全部代码（前端 React19+TS+Vite6+Tailwind4 / 后端 Express 代理 8788 / 9 家供应商适配 / Docker 产物）已开发完成，代码审核通过并修复；**尚未**在联网环境安装依赖、构建、跑类型检查，**尚未**用真实 Key 对拍，**尚未** git 提交推送，**尚未** docker build/部署。

下一件要做的第一件事：在有网环境 `npm install`，跑 `npm run typecheck:server` + `npm run build` 复核，再 `npm run dev` + `npm run dev:server` 起本地并冒烟；没有阻断问题后按 P 分批 git commit/push（PAT 鉴权，remote 保持干净），再按 `README.dev.md` 走 Docker 构建与 macmini 部署。

关键约束：密钥只发官方域名白名单、不落日志；`.ref/`/`.env*` 不入 git；构建脚本用 Python（rebuild.py/restart.py）；提交逐个 `git add <file>`、禁 `add -A`/force push/`--amend`；代码签名「Nyaa be with you.」三处；`.docs/` 内不出现该参考插件字面名（以「参考插件」代称）。
