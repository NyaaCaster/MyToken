# MyToken 环境变量示例（复制为 .env 使用）。
# .env / .env.* 已被 .gitignore 排除，禁止提交任何真实值（.env.example 除外）。

# ---- 私有 Docker 镜像仓库地址（构建/部署用；不同环境取值不同，见工作区 CLAUDE.md）----
#   Windows 研发构建推送：本机回环地址的官方端口（避免 NAT hairpin 推送失败）
#   macmini 部署拉取：Windows 宿主机局域网 IP:官方端口（直连不走外网）
#   公网域名:官方端口 仅对外公开访问，不用于构建/部署
# 被 rebuild.py 读取用于 build+push；被 docker-compose.publish.yml 的镜像地址引用。
# PRIVATE_DOCKER_REGISTRY_HOST=replace-with-your-registry-host

# ---- 前端 web 服务暴露到宿主机端口（docker-compose(.publish).yml 的 web.ports 宿主侧）----
# WEB_PORT=5200

# ---- 后端 Express 代理暴露到宿主机端口（默认 5201 → 容器内 8788）----
# SERVER_PORT=5201
